questions:

# General
- variable: global.baseDomain
  description: >-
    Base domain for all services. All service hostnames are derived as
    <service>.baseDomain (e.g., superset.example.openg2p.org).
  type: string
  label: Base Domain
  required: true
  group: General

# Keycloak
- variable: global.keycloakBaseUrl
  description: Keycloak Base URL used in all components (e.g. https://keycloak.openg2p.org)
  type: string
  label: Keycloak Base URL
  group: Keycloak

- variable: keycloak-init.enabled
  description: >-
    Enable Keycloak client initialization. Creates OIDC clients and secrets
    for Superset, OpenSearch, Kafka, MinIO, and ODK.
  type: boolean
  label: Run Keycloak Init?
  group: Keycloak

- variable: keycloak-init.keycloak.user
  description: >-
    Keycloak user with client management permissions (roles: manage-clients,
    query-clients, view-clients in master realm).
  type: string
  label: Keycloak Client Manager Username
  required: true
  group: Keycloak

- variable: keycloak-init.keycloak.password
  description: >-
    Password for the Keycloak client manager user. If provided, the chart
    creates the keycloak-client-manager Kubernetes secret automatically.
    Leave blank if you have already created the secret manually.
  type: password
  label: Keycloak Client Manager Password
  group: Keycloak

# PostgreSQL
- variable: postgresql.enabled
  description: Disable for external PostgreSQL server
  type: boolean
  label: Install PostgreSQL
  group: Postgres

- variable: global.postgresqlHost
  description: Hostname of PostgreSQL installation. Change this only for external PostgreSQL server.
  type: string
  label: PostgreSQL Hostname
  group: Postgres

- variable: postgres-init.postgresql.existingSecret
  description: >-
    Name of the Kubernetes secret containing the PostgreSQL superuser password.
    Change this when using an external PostgreSQL server.
  type: string
  label: PostgreSQL Password Secret Name
  group: Postgres

# Mail
- variable: mail.enabled
  description: This installs Mail server for relaying SMTP requests to preferred backend.
  type: boolean
  label: Install Mail SMTP Server?
  group: Mail

- variable: global.mailInstallationName
  description: Hostname of the Mail server. Change for external installations.
  type: string
  label: Mail Server Hostname
  group: Mail

# Minio
- variable: minio.enabled
  description: This installs Minio for document store.
  type: boolean
  label: Install Minio?
  group: Minio

# Opensearch
- variable: opensearch.enabled
  description: This installs OpenSearch.
  type: boolean
  label: Install OpenSearch?
  group: Opensearch

# Kafka
- variable: kafka.enabled
  description: This installs Kafka.
  type: boolean
  label: Install Kafka?
  group: Kafka

- variable: kafkaUi.enabled
  description: This installs Kafka UI
  type: boolean
  label: Install Kafka UI?
  group: Kafka
